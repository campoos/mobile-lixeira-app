package com.example.smarthtrashapp

import android.app.Activity
import android.os.Bundle

// Activity de template gerada pelo projeto inicial.
// Não é utilizada pelo aplicativo SmartTrash em produção,
// mas é mantida vazia apenas para não quebrar o build.
class MainActivity : Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Nenhuma UI aqui – a MainActivity real está em com.smarttrash.app.ui.main.MainActivity
    }
}